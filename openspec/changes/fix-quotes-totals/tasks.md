## 1. Quote totals rendering

- [x] 1.1 Remove the misplaced totals markup before the quote document structure and place the existing totals preview inside the Vue-managed quote form.
- [x] 1.2 Add a regression test that verifies totals bindings are inside `#quotes-app`, remain localized, and are not duplicated outside the Vue root.
- [x] 1.3 Run quote UI tests, full Maven verification, and OpenSpec strict validation.
